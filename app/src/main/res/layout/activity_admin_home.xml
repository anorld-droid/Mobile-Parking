<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.admin.AdminHomeActivity">


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/current_bookings_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="56dp"
        android:layout_gravity="fill"
        tools:listitem="@layout/admin_current_space_item"
        />
    <ProgressBar
        android:id="@+id/loading_progress_bar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

    <TextView
        android:id="@+id/no_bookings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/no_occupied_slots"
        android:textColor="@android:color/black"
        android:textSize="20sp"
        android:layout_gravity="center" />
    <Button
        android:id="@+id/generate_report"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="30dp"
        android:layout_weight="0"
        android:background="@drawable/cornered_rectangle_black_backgroud"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:text="@string/generate_report"
        android:textColor="@color/color_white"
        android:textSize="12sp"
        android:textStyle="bold"
         />

</androidx.coordinatorlayout.widget.CoordinatorLayout>